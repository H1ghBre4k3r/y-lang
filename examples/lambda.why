fn main(): i64 {
    let double = get_lambda();
    let add_one = create_add_x(1);
    let res = add_one(42);
    double(res)
}

fn create_add_x(x: i64): (i64) -> i64 {
    \(y) => x + y
}

fn get_lambda(): (i64) -> i64 {
    \(x) => x * 2
}
