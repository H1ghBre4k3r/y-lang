fn main(): i64 {
    42
}

const PI: f64 = 3.14159;

struct Point {
    x: i64;
    y: i64;
}

fn add(a: i64, b: i64): i64 {
    {
        1337;
        let foo = \(x) => {
            42;
            let x = Point {
                x: 1337,
                y: 42
            };
        };
    };
    a + b
}

fn test_lambda(): void {
    let square = \(x) => x * x;
    let result = square(5);
}
